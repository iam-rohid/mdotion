import AppSideBarHeader from "./header/Header";

const SideBar = () => {
  return (
    <div className="sidebar">
      <AppSideBarHeader />
      AppSideBar
    </div>
  );
};

export default SideBar;
